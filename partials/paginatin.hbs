<nav class="pagination" role="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{pageUrl prev}}">&larr; Newer Posts</a>
    {{/if}}
    <span class="page-number">Page {{page}} {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{pageUrl next}}">Older &rarr;</a>
    {{/if}}
</nav>
